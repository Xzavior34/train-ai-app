# Copy this file to .env.local and fill in your own Supabase project's values.
# Find these under Project Settings -> API in your Supabase dashboard.
# Never commit the real .env.local file — it's already in .gitignore.
#
# This app now shares the same Supabase project (and data) as train-ai-ltd-main.
# Real values live in the untracked .env file at the repo root.

VITE_SUPABASE_URL=https://qibqouymqtpirtbyjvjr.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
